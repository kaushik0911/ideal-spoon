apiVersion: v1
kind: Service
metadata:
  name: consumer-service
spec:
  selector:
    app: consumer-service
    type: ClusterIP
  # ports:
  #   - protocol: TCP
  #     port: 80
  #     targetPort: 80
